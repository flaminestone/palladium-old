<!DOCTYPE html>
<html>
<head>
  <title>Palladium</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-default navbar-inverse-app">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href= <%= root_path %>>Palladium</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle " data-toggle="dropdown" style = "background-color: #000000">
                <div class="user_name"><%= current_user.email %> <b class="caret"></b></div>

              </a>
              <ul class="dropdown-menu">
                <li>
                  <a href="/settings/admin"><i class="fa fa-fw fa-gear"></i> <%= t("helpers.links.settings") %></a>
                </li>
                <li class="divider"></li>
                <li>
                  <%= link_to "<i class='fa fa-fw fa-power-off'></i> #{t("helpers.links.logout")}".html_safe, destroy_user_session_path, :method => :delete %>
                </li>
              </ul>
            </li>
        <% end %>
      </ul>
    </div>
</nav>
<%= yield %>
</body>
</html>