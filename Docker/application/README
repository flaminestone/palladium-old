For install docker
sudo apt-get install curl
curl -fsSL https://get.docker.com/ | sh
curl -fsSL https://get.docker.com/gpg | sudo apt-key add -

For install palladium:
sudo docker build -t palladium .
sudo docker create -i -t --volumes-from dbdata --name palladium palladium:latest
sudo docker start palladium




